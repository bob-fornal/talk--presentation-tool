import { House } from './hard-to-test.js';

describe('House Class (Hard to Test)', () => {
  let house;

  beforeEach(() => {
    house = new House();
  });

  it('expects kitchen and bedroom to be defined', () => {
    // The objects were created at instantiation
  });
});