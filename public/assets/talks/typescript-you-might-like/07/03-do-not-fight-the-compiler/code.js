// The ANY escpae hatch
function hackyFunction(data: any) {  // Giving up
  return data.whatever.you.want;
}

// Ask "why is this hard?"
// * Often means your types don't match reality
// * Refactor the types, not add "any"
